import React, { StyleSheet } from 'react-native'

export default StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        flexDirection: 'row',
        padding: 20,
    },
    button: {
        margin: 10,
        marginTop: 25,
        alignItems: 'center',
        justifyContent: 'center',
        flex: 1
    },
    inputFields: {
        marginTop: 15,
    }
});